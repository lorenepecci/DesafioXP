"use strict";
/* import { Decimal } from '@prisma/client/runtime';
import { expect } from 'chai';
import { AtivosModel } from '../../../models/ativosModel';

describe('--- Testes no MODEL ativos ---', () => {
  const model = new AtivosModel();
/*   const prisma = new PrismaClient();
  describe('Testando a função create', () => {
    beforeEach(() => {
      const ativoCriado = {
        codAtivo: 5,
        qtdeAtivo: 2,
        valorAtivo: new Decimal(10),
      };
      sinon.stub(prisma, 'create').resolves(ativoCriado);
    });

    afterEach(() => {
      (model.create as sinon.SinonStub).restore();
    });

    it('Sucesso de criar um ativo ', async () => {
      const response = await model.create({
        qtdeAtivo: 2,
        valorAtivo: new Decimal(10),
      });
      expect(response).to.be.an('object');
    });
  });
});
 */ 
//# sourceMappingURL=ativoM.test.js.map